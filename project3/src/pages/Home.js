import { useSearchParams } from "react-router-dom";
import Button from "../component/Button";

const Home = () => {

    return (
        <div>
            <Button></Button>
        </div>
    )
};

export default Home;